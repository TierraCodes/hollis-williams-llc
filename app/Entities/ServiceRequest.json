{
  "name": "ServiceRequest",
  "type": "object",
  "properties": {
    "customer_name": {
      "type": "string"
    },
    "customer_email": {
      "type": "string",
      "format": "email"
    },
    "customer_phone": {
      "type": "string"
    },
    "service_address": {
      "type": "string"
    },
    "property_size": {
      "type": "string",
      "enum": [
        "small",
        "medium",
        "large",
        "extra_large"
      ]
    },
    "service_type": {
      "type": "string",
      "enum": [
        "mowing",
        "trimming",
        "edging",
        "mulching",
        "leaf_removal",
        "spring_cleanup",
        "fall_cleanup",
        "aeration",
        "seeding",
        "landscape_installation",
        "tree_service",
        "consultation",
        "other"
      ]
    },
    "preferred_date": {
      "type": "string",
      "format": "date"
    },
    "time_preference": {
      "type": "string",
      "enum": [
        "morning",
        "afternoon",
        "flexible"
      ]
    },
    "description": {
      "type": "string",
      "description": "Detailed description of the service needed"
    },
    "photos": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "URLs of uploaded property photos"
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "reviewed",
        "scheduled",
        "completed",
        "cancelled"
      ],
      "default": "pending"
    },
    "estimated_cost": {
      "type": "number",
      "description": "Estimated cost provided by business"
    },
    "scheduled_date": {
      "type": "string",
      "format": "date"
    },
    "admin_notes": {
      "type": "string",
      "description": "Internal notes from Hollis Williams team"
    }
  },
  "required": [
    "customer_name",
    "customer_phone",
    "service_address",
    "service_type"
  ]
}